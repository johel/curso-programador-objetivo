let lista = [1, 2, 3, 4, 5];

function printReverse(listaQualquer) {
  for (let i = listaQualquer.length - 1; i >= 0; i = i - 1) {
    console.log(listaQualquer[i]);
  }
}

printReverse(lista);
console.log("fdgf");
